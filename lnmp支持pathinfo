//ubuntu下路径为 /etc/nginx//sites-available/default 文件， 修改完重启nginx
location / {
  try_files $uri $uri/ =404;
}

location /checkweb/ {
  if (!-e $request_filename){
    rewrite  ^/checkweb/(.*)$  /checkweb/index.php?s=$1  last;
  }
}
